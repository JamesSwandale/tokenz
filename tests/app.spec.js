"use strict";
var chai = require('chai'),
	assert = chai.assert,
	should = require('should'),
	request = require('supertest'),
	app = '';


describe('Tokens tests', function() {
	describe('', function() {
		it('', function(done) {
		});
		it('', function(done) {
		});
	})

	describe('', function() {
		it('', function(done) {
		});
		it('', function(done) {
		});
	})

	describe('', function() {
		it('', function(done) {
		});
		it('', function(done) {
		});
	})

	describe('', function() {
		it('', function(done) {
		});
		it('', function(done) {
		});
	})

});
